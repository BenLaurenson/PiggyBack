name: CI

on:
  push:
    branches: [main, dev]
  pull_request:
    branches: [main, dev]

permissions:
  contents: read

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v4
      - uses: actions/setup-node@53b83947a5a98c8d113130e565377fae1a50d02f # v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - run: npm run test:run

  build:
    name: Build
    needs: [test]
    runs-on: ubuntu-latest
    env:
      # Placeholders so `next build` can resolve module-level env reads. The
      # build doesn't actually call Supabase/Stripe; values are only checked
      # for shape (URLs / hex lengths).
      NEXT_PUBLIC_SUPABASE_URL: https://placeholder.supabase.co
      NEXT_PUBLIC_SUPABASE_ANON_KEY: placeholder-anon-key
      SUPABASE_SERVICE_ROLE_KEY: placeholder-service-role-key
      UP_API_ENCRYPTION_KEY: 0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef
      PROVISIONER_ENCRYPTION_KEY: 0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v4
      - uses: actions/setup-node@53b83947a5a98c8d113130e565377fae1a50d02f # v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - run: npm run build

  # Lint is intentionally NOT a CI gate today.
  # eslint 9.39.2 + @eslint/eslintrc 3.3.3 + ajv have a known incompatibility
  # ("NOT SUPPORTED: option missingRefs"). The lint step crashes before any
  # rule runs — it's a config bug, not a real lint failure. Will re-enable
  # once the eslint config is migrated off the legacy ajv path.
